import { Highlightone } from './highlightone';

describe('Highlightone', () => {
  it('should create an instance', () => {
    const directive = new Highlightone();
    expect(directive).toBeTruthy();
  });
});
